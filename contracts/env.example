# Optimism-goerli
OPTIMISM_GOERLI_RPC_URL=
PRIVATE_KEY=
# When verify on etherscan-optimism, it also use this api key, not `OPTIMISM_ETHERSCAN_API_KEY`
ETHERSCAN_API_KEY=
OPTIMISM_ETHERSCAN_API_KEY=
LIGHT_CLIENT_ADDRESS=
# OutputOracle contract address on Base-goerli
OUTPUT_ORACLE=
CRC_INBOX=
# outbox contract on the opposite side(base-goerli)
OUTBOX_ADDRESS=
# chain id of the opposite side(base-goerli)
CHAIN_ID=


# Base-goerli
# OPTIMISM_GOERLI_RPC_URL=
# PRIVATE_KEY=
# ETHERSCAN_API_KEY=
# # When verify on etherscan-base, it also use this api key, not `OPTIMISM_ETHERSCAN_API_KEY`
# OPTIMISM_ETHERSCAN_API_KEY=
# LIGHT_CLIENT_ADDRESS=
# # OutputOracle contract address on Optimism-goerli
# OUTPUT_ORACLE=
# CRC_INBOX=
# # outbox contract on the opposite side(base-goerli)
# OUTBOX_ADDRESS=
# # chain id of the opposite side(base-goerli)
# CHAIN_ID=